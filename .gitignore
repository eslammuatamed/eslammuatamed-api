# compiled output
/dist
/node_modules
/build

# Prisma Client — generated TypeScript source (Prisma 7 emits it into the repo
# instead of node_modules). Regenerate with `npm run db:generate`; CI and the
# deploy workflow already run it after `npm ci`. Never edit or commit it.
/src/generated/prisma

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# temp directory
.temp
.tmp

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# ignore AI folders
.omc
.claude

# SpecKit transient active-feature pointer (local state; specs live in .specify/specs/)
.specify/feature.json
